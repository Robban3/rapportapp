# Kopiera denna fil till .env.local och fyll i värdena från ditt Supabase-projekt
# (Project Settings -> API). Utan dessa kör appen mot inbyggd seed-data (mock),
# så du kan starta direkt med `npm run dev` innan Supabase är uppsatt.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
